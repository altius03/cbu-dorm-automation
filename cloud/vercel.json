{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm ci --ignore-scripts --no-fund",
  "buildCommand": "npm run build",
  "regions": ["icn1"],
  "fluid": true,
  "crons": [{ "path": "/api/cron/holidays", "schedule": "0 18 * * *" }]
}
